# Direct dependencies only — pip resolves the rest (Werkzeug, Jinja2,
# numpy, pytz, soupsieve, urllib3's own transitive deps, etc.).
# Refreshed 2026-07-05; previous file was pinned to 2022-era versions and
# was missing playwright/lxml/html5lib despite both being imported.
# Targets PythonAnywhere (Linux) — Windows-only packages (pywin32,
# win-inet-pton, PySocks, brotlipy) intentionally excluded.

# Web app
Flask==3.1.3
Flask-Mail==0.10.0

# Scraping
beautifulsoup4==4.15.0
lxml==6.1.1
html5lib==1.1
requests==2.34.2
urllib3==2.7.0

# playwright is pinned to the version already verified working locally,
# not the latest, since browser binaries are tied to this exact version —
# run `playwright install chromium` after any bump.
playwright==1.55.0

# Data handling
pandas==2.3.3

# Date parsing — not yet used in code; staged for ROADMAP.md §2
python-dateutil==2.9.0.post0
